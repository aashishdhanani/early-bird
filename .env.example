# Your email address to receive the daily digest
EMAIL_RECIPIENT=your_email@example.com

# Email delivery method: "gmail_api" or "smtp"
# gmail_api = Uses Gmail API (no password needed, requires OAuth setup)
# smtp = Uses SMTP (requires SENDGRID_API_KEY or Gmail app password)
EMAIL_METHOD=gmail_api

# === Gmail API Setup (recommended) ===
# 1. No password needed! Uses OAuth2
# 2. Follow setup instructions in GMAIL_API_SETUP.md
# 3. Email will be sent from your Gmail account

# === SMTP Setup (alternative) ===
# For SendGrid: Get API key from https://app.sendgrid.com/settings/api_keys
SENDGRID_API_KEY=your_sendgrid_api_key_here

# For Gmail SMTP: Requires app-specific password (needs 2FA)
EMAIL_SENDER=your_gmail@gmail.com

# === AI Summaries (optional) ===
# Get API key from: https://platform.openai.com/api-keys
# Enables intelligent summaries that explain what each article means for AI/robotics
# Leave blank to use original article descriptions
OPENAI_API_KEY=
